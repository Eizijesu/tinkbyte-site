---
import Layout from '../layouts/Layout.astro';

const categories = [
  {
    slug: 'tech-culture',
    name: 'Tech Culture',
    description:
      'Community stories, ethical design discussions, and the human side of building technology.',
    icon: '🏛️',
    color: 'bg-blue-50 dark:bg-blue-900/20 border-blue-200 dark:border-blue-800',
  },
  {
    slug: 'ai-evolution',
    name: 'AI Evolution',
    description:
      'Practical AI education cutting through the hype with real-world applications and strategic guidance.',
    icon: '🤖',
    color: 'bg-purple-50 dark:bg-purple-900/20 border-purple-200 dark:border-purple-800',
  },
  {
    slug: 'product-insights',
    name: 'Product Insights',
    description:
      'How products actually get made—decisions, tradeoffs, launches, and feedback loops.',
    icon: '📦',
    color: 'bg-green-50 dark:bg-green-900/20 border-green-200 dark:border-green-800',
  },
  {
    slug: 'startup-lessons',
    name: 'Startup Lessons',
    description:
      'Real lessons from real failures, MVPs, and test launches. Intelligent failures and rapid iteration.',
    icon: '🚀',
    color: 'bg-orange-50 dark:bg-orange-900/20 border-orange-200 dark:border-orange-800',
  },
  {
    slug: 'tools',
    name: 'Tools & Resources',
    description:
      'Honest reviews and practical frameworks for tools that actually help builders create better products.',
    icon: '🛠️',
    color: 'bg-indigo-50 dark:bg-indigo-900/20 border-indigo-200 dark:border-indigo-800',
  },
  {
    slug: 'industry-news',
    name: 'Industry News',
    description: 'Weekly analysis of trends and news that actually matters. Signal from the noise.',
    icon: '📰',
    color: 'bg-red-50 dark:bg-red-900/20 border-red-200 dark:border-red-800',
  },
];

const pageTitle = 'Features | TinkByte';
const pageDescription =
  "Explore TinkByte's feature articles across all categories - from tech culture to AI evolution, product insights to startup lessons.";
---

<Layout title={pageTitle}>
  <!-- Hero Section with matching style -->
  <section class="theme-transition-all px-4 py-10 sm:px-6 sm:py-16 md:py-20">
    <div class="relative mx-auto max-w-2xl">
      <!-- Decorative blobs matching homepage -->
      <div
        class="animate-blob theme-transition-bg absolute -left-10 -top-10 h-40 w-40 rounded-full bg-zinc-100 opacity-50 blur-3xl dark:bg-zinc-800/50 sm:-left-20 sm:-top-20 sm:h-64 sm:w-64"
      >
      </div>
      <div
        class="animate-blob animation-delay-2000 theme-transition-bg absolute -bottom-10 -right-10 h-40 w-40 rounded-full bg-zinc-200 opacity-30 blur-3xl dark:bg-zinc-800/30 sm:-bottom-20 sm:-right-20 sm:h-64 sm:w-64"
      >
      </div>

      <div class="relative text-center sm:text-left">
        <h1
          class="theme-transition-color hero-text text-3xl font-bold tracking-tight text-zinc-900 dark:text-zinc-100 sm:text-4xl md:text-5xl lg:text-6xl"
        >
          <span class="block">Features that</span>
          <span class="mt-1 block">cut through</span>
          <span class="relative mt-1 block">
            <span class="relative inline-block">
              the noise.
              <span
                class="theme-transition-bg absolute -bottom-1 left-0 h-1 w-full origin-left transform bg-zinc-800 dark:bg-zinc-200"
              ></span>
            </span>
          </span>
        </h1>
        <p
          class="theme-transition-color mx-auto mt-4 max-w-lg text-base leading-relaxed text-zinc-600 dark:text-zinc-400 sm:mx-0 sm:mt-6 sm:text-lg md:mt-8"
        >
          Authentic insights for builders who value substance over hype. Explore our curated content
          across six key areas of technology and product development.
        </p>
      </div>
    </div>
  </section>

  <!-- Categories Section -->
  <section
    class="theme-transition-all border-t border-zinc-100 px-4 py-10 dark:border-zinc-800 sm:px-6 sm:py-12 md:py-16"
  >
    <div class="mx-auto max-w-6xl">
      <div
        class="mb-6 flex flex-col justify-between gap-4 sm:mb-8 sm:flex-row sm:items-center md:mb-12"
      >
        <h2
          class="theme-transition-color text-center text-xl font-bold tracking-tight text-zinc-900 dark:text-zinc-100 sm:text-left sm:text-2xl md:text-3xl"
        >
          Content Categories
        </h2>
      </div>

      <!-- Categories Grid with matching card style -->
      <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 sm:gap-8 md:gap-12 lg:grid-cols-3">
        {
          categories.map((category, index) => (
            <article class="hover-3d theme-transition-element group relative mx-auto flex w-full max-w-sm flex-col items-start sm:mx-0">
              <div class="theme-transition-bg absolute -inset-x-4 -inset-y-6 z-0 scale-95 bg-zinc-50 opacity-0 transition group-hover:scale-100 group-hover:opacity-100 dark:bg-zinc-800/50 sm:-inset-x-6 sm:rounded-2xl" />

              <div class="relative z-10 mb-6 w-full text-center">
                <div class="mb-4 text-4xl">{category.icon}</div>
                <h3 class="theme-transition-color text-xl font-semibold tracking-tight text-zinc-900 transition-colors group-hover:text-zinc-700 dark:text-zinc-100 dark:group-hover:text-zinc-300 sm:text-2xl">
                  {category.name}
                </h3>
              </div>

              <p class="theme-transition-color relative z-10 mb-6 line-clamp-3 w-full text-center text-sm text-zinc-600 dark:text-zinc-400">
                {category.description}
              </p>

              <div class="relative z-10 flex w-full flex-col gap-3">
                <a
                  href={`/category/${category.slug}`}
                  class="group-link theme-transition-color relative mx-auto flex min-h-[44px] items-center text-sm font-medium text-zinc-700 transition-colors group-hover:text-zinc-900 dark:text-zinc-300 dark:group-hover:text-zinc-100"
                >
                  <span class="relative inline-block overflow-hidden">
                    <span class="block transition-transform duration-300 group-hover:-translate-y-full">
                      View Articles
                    </span>
                    <span class="absolute left-0 top-0 translate-y-full whitespace-nowrap transition-transform duration-300 group-hover:translate-y-0">
                      Explore now
                    </span>
                  </span>
                  <svg
                    viewBox="0 0 16 16"
                    fill="none"
                    aria-hidden="true"
                    class="ml-1 h-4 w-4 stroke-current transition-transform duration-300 group-hover:translate-x-1"
                  >
                    <path
                      d="M6.75 5.75 9.25 8l-2.5 2.25"
                      stroke-width="1.5"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    />
                  </svg>
                </a>

                <div class="text-center text-xs text-zinc-500 dark:text-zinc-400">Coming soon</div>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Coming Soon Section -->
  <section
    class="theme-transition-all border-t border-zinc-100 px-4 py-10 dark:border-zinc-800 sm:px-6 sm:py-12 md:py-16"
  >
    <div class="mx-auto max-w-3xl text-center">
      <h2
        class="theme-transition-color mb-6 text-xl font-bold tracking-tight text-zinc-900 dark:text-zinc-100 sm:mb-8 sm:text-2xl md:text-3xl"
      >
        Latest Featured Articles
      </h2>

      <div class="relative">
        <div
          class="theme-transition-bg absolute -inset-x-4 -inset-y-6 z-0 rounded-2xl bg-zinc-50 dark:bg-zinc-800/50"
        >
        </div>
        <div class="relative z-10 p-8 sm:p-12">
          <div class="mb-6 text-4xl sm:text-6xl">📝</div>
          <h3 class="mb-4 text-xl font-semibold text-zinc-900 dark:text-zinc-100 sm:text-2xl">
            Content Coming Soon
          </h3>
          <p class="mx-auto mb-8 max-w-2xl leading-relaxed text-zinc-600 dark:text-zinc-400">
            We're working on bringing you authentic insights and practical frameworks. Subscribe to
            our newsletter to be notified when we publish our first articles.
          </p>

          <a
            href="/#newsletter"
            class="theme-transition-color group relative inline-flex min-h-[44px] items-center gap-2 text-sm font-medium text-zinc-900 transition-all duration-300 hover:text-zinc-700 dark:text-zinc-100 dark:hover:text-zinc-300"
          >
            <span>Subscribe for Updates</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
              class="h-4 w-4 transition-transform duration-300 group-hover:translate-x-1"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path>
            </svg>
            <span
              class="theme-transition-bg absolute -bottom-1 left-0 h-0.5 w-0 bg-zinc-800 transition-all duration-300 group-hover:w-full dark:bg-zinc-200"
            ></span>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Matching animations from homepage */
  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }

  .hover-3d {
    transition: transform 0.3s ease;
  }

  .hover-3d:hover {
    transform: translateY(-2px);
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Content reveal animations */
  .hero-text span {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.8s ease,
      transform 0.8s ease;
  }

  .animate-reveal {
    opacity: 1 !important;
    transform: translateY(0) !important;
  }
</style>

<script>
  // Matching script from homepage
  document.addEventListener('DOMContentLoaded', () => {
    const animateContent = () => {
      const heroElements = document.querySelectorAll('.hero-text span, .hero-text + p');
      heroElements.forEach((el, index) => {
        setTimeout(
          () => {
            el.classList.add('animate-reveal');
          },
          100 + index * 150
        );
      });

      const articles = document.querySelectorAll('article.group');
      articles.forEach((article, index) => {
        setTimeout(
          () => {
            article.classList.add('animate-reveal');
          },
          500 + index * 150
        );
      });
    };

    animateContent();
  });
</script>
